apiVersion: v1
kind: Service
metadata:
  name: notification-api
  namespace: insider
spec:
  selector:
    app: notification-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
